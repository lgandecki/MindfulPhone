// Secrets — copy this to Secrets.xcconfig and fill in your values.
// Generate a secret: openssl rand -base64 32
// Must match APP_SHARED_SECRET on the Cloudflare Worker.

APP_SHARED_SECRET = your-shared-secret-here
